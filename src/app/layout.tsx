import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Global Exterminating Inc | Professional Pest Control in Berlin, MD",
  description: "Celebrating 25+ years of excellence. Professional pest control, termite inspections, mold remediation, and more in Berlin, MD. Licensed & insured. Call (410) 208-2515.",
  keywords: "pest control, termite inspection, mold remediation, exterminator, Berlin MD, Eastern Shore, mosquito treatment, tick control",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
